# Contact Form Configuration
# Copy this file to .env and configure your settings for local development
# For Cloudflare Pages deployment, set these variables in the CF Pages dashboard

# Email address where contact form submissions should be sent
# If not set, submissions will be logged to console only
CONTACT_EMAIL_TO=your-email@example.com

# Email service configuration (future use)
# Uncomment and configure based on your email service
# CONTACT_EMAIL_SERVICE=sendgrid
# CONTACT_SENDGRID_API_KEY=your-sendgrid-api-key
# CONTACT_FROM_EMAIL=noreply@yourdomain.com
# CONTACT_FROM_NAME=Your Website

# Alternative service examples:
# CONTACT_EMAIL_SERVICE=ses
# CONTACT_AWS_REGION=us-east-1
# CONTACT_AWS_ACCESS_KEY_ID=your-access-key
# CONTACT_AWS_SECRET_ACCESS_KEY=your-secret-key

# Rate limiting configuration (optional)
# CONTACT_RATE_LIMIT_WINDOW=900000  # 15 minutes in milliseconds
# CONTACT_RATE_LIMIT_MAX_REQUESTS=5  # Max requests per window

# Cloudflare Pages Deployment Instructions:
# 1. Go to your Cloudflare Pages dashboard
# 2. Navigate to your project > Settings > Environment variables
# 3. Add the following environment variables for production:
#    - CONTACT_EMAIL_TO=your-email@example.com
#    - CONTACT_EMAIL_SERVICE=your-preferred-service (optional)
#    - Any API keys for your email service (if using)
# 4. For preview deployments, add the same variables under "Preview" section
#
# Note: Environment variables in CF Pages are available at runtime
# and work seamlessly with the @astrojs/cloudflare adapter